<%= form_for(@business) do |f| %>
  <% if @business.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business.errors.count, "error") %> prohibited this deal from being saved:</h2>

      <ul>
      <% @business.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :about %><br>
    <%= f.text_field :about %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.telephone_field :phone_number %>
  </div>
  <div class="field">
    <%= f.label :full_address %><br>
    <%= f.fields_for :full_address do |a| %>
     <p> <%= a.object.label %> </p>
     <%= a.text_field :street %>
     <%= a.text_field :city %>
     <%= a.text_field :state %>
     <%= a.text_field :zip %>
   <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
